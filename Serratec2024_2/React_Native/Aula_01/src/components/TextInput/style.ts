import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  boxInput: {
    width: '100%',
  },

  input: {
    backgroundColor: "#fff",
    width: "100%",
    height: 55,
    borderRadius: 5,
    paddingLeft: 10,
    fontSize: 22,
    paddingRight: 45,
  },

  boxIcon: {
    position: 'absolute',
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
    right: 0,
    width: 40,
  }
});